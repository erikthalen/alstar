{
  "name": "@alstar/shared",
  "type": "module",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "ci:publish": "pnpm publish -r",
    "docs:dev": "pnpm -r --parallel run docs:dev",
    "docs:build": "pnpm -r --parallel run docs:build"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.5",
    "prettier": "^3.6.2"
  }
}
