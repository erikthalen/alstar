/**
 * Parses a delimited set of tokens and returns an array with all whitespace and empty items removed.
 */
export declare function parseDelimitedTokens(input: string | number, delimiter?: string): string[];
